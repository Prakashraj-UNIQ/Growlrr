"use client";
import Banner from "@/components/ui/Banner";
import Rough from "@/components/ui/Rough";


/* ----------------------------- Page ------------------------------------- */

export default function CrowdFundPage() {
  //   const [selected, setSelected] = useState<string[]>([]);

  return (
    <>
      <Banner
        title="Growlrr Crowd Fund"
        subtitle="Feed the Future, Back Science-Driven Pet Nutrition"
        description="Help us bring rotation-based, human-grade meals to every pet parent. Choose a tier, unlock perks, and join our founding community."
      />
      <Rough/>

    </>
  );
}
